import React from "react";
import classes from "../../Styles/Footer.module.css"

const Footer = () => {
    return (
        <footer className={classes.bottom}>
            <p className="text-center text-light">
                Movie List | EPITA SPRING '22
            </p>
        </footer>
    );
};

export default Footer;
